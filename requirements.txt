# DEPRECATED — use `uv sync` instead (reads from pyproject.toml)
#
# For legacy pip users:
#   pip install -r requirements.txt
#
# For uv users:
#   uv sync              # install project + deps
#   uv sync --dev        # include dev deps (pytest, mypy, ruff)
#   uv tool install .    # install `cortex` as a global CLI command

textual>=0.80.0
python-dotenv>=1.0.0

# Dev
pytest>=8.0.0
pytest-asyncio>=0.24.0
mypy>=1.10.0
ruff>=0.6.0
